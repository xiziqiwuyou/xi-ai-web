# Implementation Plan

1. Add a bounded server JSONL image timing store and recent-10 estimate tests.
2. Add the public estimate route and record successful image generation durations.
3. Add shared frontend types and API methods for the estimate contract.
4. Replace Image Studio IndexedDB timing state with server estimate loading and response refresh.
5. Update API harnesses, UI/static contracts, and focused image browser coverage.
6. Run TypeScript, server/security tests, UI contracts, focused Playwright, production build, privacy scan, task validation, and diff checks.
