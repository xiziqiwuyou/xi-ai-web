# 执行计划

## P0 - 阶段确认与工作区基线

- [x] 记录 `v0.0.1` 至 `v0.0.5` 的发布链路、最新提交和当前未提交路径。
- [x] 确认当前任务只包含同步布局与回归验证；越界差异停止并上报。
- [x] 读取前端组件、状态管理和质量规范，保留与跨设备同步有关的约束。

Gate: 版本阶段、任务范围和脏文件清单可复核。

## P1 - 同步布局收口

- [x] 保持正文单一滚动所有者。
- [x] 固定标签栏高度并使正文自然高度从顶部开始。
- [x] 保持桌面固定尺寸、移动端安全区和现有协议行为。
- [x] 只保留轻量淡入，消除状态切换造成的几何位移。

Gate: 手动检查桌面和移动端截图，确认没有大段顶部空白、外框跳动或常驻滚动条。

## P2 - 回归测试与监督配置

- [x] 固化桌面方向/接收方式切换几何契约。
- [x] 固化审批首屏可见、二维码区域内、正文不溢出和焦点契约。
- [x] 固化真实滚动后显示、空闲恢复隐藏的滚动条契约。
- [x] 创建并审查 `xi-ai-web-supervisor` 只读智能体配置。

Gate: 同步专用 Playwright 全部通过，测试断言不依赖放宽阈值掩盖布局问题。

## P3 - 全局质量门禁

- [x] 运行 `npm run check`。
- [x] 运行 `npm run build`。
- [x] 运行 `npm run privacy`。
- [x] 运行 `npm run release-check`。
- [x] 运行 `git diff --check`。

Gate: 所有命令通过，或将明确的环境缺口记录为阻塞项。

## P4 - 发布准备评审

- [ ] 使用监督智能体做一次全范围只读审查。
- [x] 检查版本文件、Compose 镜像引用和发布文档是否仍与当前标签一致。
- [x] 形成下一版本候选说明，但不在本任务内推送或部署。

Gate: 无 P0/P1 发现、无越界文件、无未解释的测试缺口。

## P5 - 交接下一阶段

- [x] 把核心对话/图片生成上线验收单独创建为新任务。
- [x] 新任务至少覆盖 API 失败、流式输出、图片字节结果、BYOK、模型目录和部署健康检查。
- [ ] 本任务完成后再进入 Trellis Finish/提交流程。

## 回滚

若布局回归失败，只回滚本任务新增的同步样式和 E2E 差异，不修改 `v0.0.5` 标签或历史发布提交。监督智能体只读，不参与回滚。
